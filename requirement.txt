# Core backend framework
fastapi==0.110.0
# NOTE: On some Windows setups pip can fail resolving `uvicorn[standard]==X` due to extras handling.
# `run_server.ps1` works fine with plain uvicorn; the "standard" deps are optional.
uvicorn==0.29.0

# Database & ORM
sqlalchemy==2.0.29
# psycopg2-binary: If installation fails on Windows, try:
#   pip install --user psycopg2-binary --only-binary :all:
#   Or install Visual C++ Build Tools first
# Note: 2.9.9 not available, using 2.9.11 (latest available)
psycopg2-binary>=2.9.11
alembic==1.13.1

# Data validation
# Note: pydantic 2.6.4 requires Rust to build pydantic-core from source
# Using flexible version to allow pip to find pre-built wheels
pydantic>=2.6.0,<3.0.0
pydantic-settings>=2.2.0,<3.0.0

# AI / LLM
google-generativeai==0.3.2


# Async & HTTP
httpx==0.27.0
aiofiles==23.2.1

# Utilities
python-dotenv==1.0.1
loguru==0.7.2
numpy==1.26.3
jira==3.5.2
PyYAML==6.0.1
sentence-transformers==2.6.1
# Allow a small range so we don't force downloads if a compatible version is already installed.
cachetools>=5.3.3,<6.0
# Redis for caching and future job queue (optional but recommended for scaling)
redis==5.0.1

# Vector Database (Pinecone for RAG)
pinecone-client==5.0.1

# LangChain / LangGraph (agents orchestration)
# This repo is compatible with LangChain 0.3.x (Pydantic v2).
langchain-core>=0.3,<0.4
langchain>=0.3,<0.4
langgraph>=0.2,<0.4

# Testing
pytest==8.1.1
pytest-asyncio==0.23.5
